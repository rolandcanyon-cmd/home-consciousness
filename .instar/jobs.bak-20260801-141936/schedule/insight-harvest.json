{
  "slug": "insight-harvest",
  "origin": "instar",
  "schedule": "0 */8 * * *",
  "priority": "medium",
  "expectedDurationMinutes": 3,
  "enabled": true,
  "execute": {
    "type": "agentmd"
  },
  "manifestVersion": 1,
  "model": "opus",
  "tags": [
    "cat:learning",
    "evolution"
  ],
  "unrestrictedTools": true,
  "gate": "curl -sf -H \"Authorization: Bearer $INSTAR_AUTH_TOKEN\" -H \"X-Instar-AgentId: $INSTAR_AGENT_ID\" http://localhost:${INSTAR_PORT:-4040}/evolution/learnings?applied=false 2>/dev/null | python3 -c \"import sys,json; d=json.load(sys.stdin); exit(0 if len(d.get('learnings',[])) > 0 else 1)\"",
  "mcpAccess": "none"
}