{
  "slug": "evolution-overdue-check",
  "origin": "instar",
  "schedule": "0 */4 * * *",
  "priority": "high",
  "expectedDurationMinutes": 2,
  "enabled": true,
  "execute": {
    "type": "agentmd"
  },
  "manifestVersion": 1,
  "model": "haiku",
  "tags": [
    "cat:learning",
    "role:worker",
    "exec:prompt",
    "pair:commitment-detection"
  ],
  "unrestrictedTools": true,
  "gate": "curl -sf -H \"Authorization: Bearer $INSTAR_AUTH_TOKEN\" -H \"X-Instar-AgentId: $INSTAR_AGENT_ID\" http://localhost:${INSTAR_PORT:-4040}/evolution/actions/overdue 2>/dev/null | python3 -c \"import sys,json; d=json.load(sys.stdin); exit(0 if len(d.get('overdue',[])) > 0 else 1)\"",
  "mcpAccess": "none"
}
